# Mensa Telegram Bot

A Telegram bot that sends the Mensa menu to users.

## Setup
1. Install dependencies: `npm install`
2. Create a `.env` file in the root with your Telegram bot token and Mensa menu URL.
3. Run the bot: `npm start`

## Project Structure
- `src/` - Source code for the bot
- `config/` - Configuration files
